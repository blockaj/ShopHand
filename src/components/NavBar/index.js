import { useState } from 'React';


const NavBar(): React.FC = ({children}) => {
    return (
        <div className="nav-bar">
            <ul>
                
            </ul>
        </div>
    );
}
